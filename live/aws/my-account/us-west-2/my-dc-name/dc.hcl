# ---------------------------------------------------------------------------------------------------------------------
# TERRAGRUNT CONFIGURATION
# ---------------------------------------------------------------------------------------------------------------------

locals {
  dc_name = "${basename(get_terragrunt_dir())}"
  path = get_path_from_repo_root()
  base_path = split("/${local.dc_name}", local.path)
}